# Cloudflare IP UFW Script

After messing around with a couple of VMs recently, I decided to make a quick shell script to allow the IP Addresses of Cloudflare.

### Running the script
```sudo ./cfufw.sh```
